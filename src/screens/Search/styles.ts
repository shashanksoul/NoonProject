import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 10,
    },
    textInput: {
        padding: 10,
        borderRadius: 4,
        borderWidth: 1,
        borderColor:'orange',
    },

});

export default styles;
