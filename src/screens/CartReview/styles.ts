import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    paymentMethodContainer: {
        paddingHorizontal: 10,
    },
    paymentMethodTitle: {fontWeight:'bold'},
});

export default styles;